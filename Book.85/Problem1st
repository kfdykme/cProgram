#include <stdio.h>
int main(void){
	
	float n = 100.0;
	float max = 0;
	
	while( n > 0){
		printf("Enter a number: ");
		scanf("%f", &n);
		
		if (n >= max){
			max = n;
		}
	}
	
	printf("The largest number is: %f\n", max);
	
	return 0;
}